platform: Common
buildPlatform:
displayName: Set Environment Variable
inputs:
- key: "AC_ENV_KEYS"
  defaultValue: "$AC_ENV_KEYS"
  isRequired: true
  title: Environment Variable Key(s)
  description: Specifies environment variable key(s) to set
  helpText: Space-separated list of environment variable keys
- key: "AC_ENV_VALUE"
  defaultValue: "$AC_ENV_VALUE"
  isRequired: false
  title: Environment Variable Value
  description: Specifies environment variable value to set
processFilename: ruby
processArguments: '%AC_STEP_TEMP%/main.rb'
files:
- "main.rb"